# REM Sleep

### Reflections
- No strong patterns surfaced.

### Possible Lasting Truths
- - `stock_financial_data`：财务报告数据（有上述 report_period 不一致问题） ## 今日调度记录 | 时间 | 任务 | 状态 | |------|------|------| | 00:21 | 财务数据同步（Tushare） | ❌ 失败（AsyncIOExecutor bug） | | 08:21 | 历史数据同步（手动） | ✅ 完成 | | 10:23 | 财务数据同步（手动） | ❌ 卡住被僵尸检测终止 | | 16:40 | 历史股票数据同步（Tushare） | ⏳ 待执行 | # 2026-04-30 记忆 ## APScheduler async job bug（已修复） **问题**：`ThreadPoolExecutor` 无法执行 `async def` 函数，导致财务数据同步等异步任务"成功执行"但实际什么都没做。 **根因**：APScheduler 的 `ThreadPoolExecutor` 收到 async 函数时，会创建一个 coroutine 对象但从不 await，任务静默失败。 **修复文件**：`skills/apps/TradingAgents-CN/app/main.py` - 改用 `AsyncIOExecutor()` 作为 default executor - `AsyncIOExecutor` 能正确处理 async def（通过事件循环）和普通 sync 函数（通过 `run_in_executor`） ## 财务数据同步 report_period 不一致 bug（已 [confidence=0.82 evidence=memory/2026-04-30.md:40-70]
- - Candidate: # 2026-04-06 Daily Memory ## Session Summary ### Investment System Setup - Working on OpenClaw investment skills: `daily_stock_analysis` and `daily-market-analysis` - Configured `litellm_config.yaml` with MiniMax + Gemini models - Set up `.env` with API keys and LITELLM_CONFIG path ### Technical Issues 1. **Network proxy issue** - 系统环境有 `socks5h://127.0.0.1:20170` 代理，导致 litellm API 调用失败 (SSL handshake failed) 2. **Module import issue** - `daily-stock-analysis` 有 `analyzer_service` 缺失问题 3. **Template mode works** - 基础数据获取（akshare）和报告生成正常 ### What's Working - ✅ akshare 数据获取正常 - ✅ 报告模板生成正常 - ✅ 股票数据复盘功能正常（上证/深证/创业板/科创50） - ❌ AI 分析未启用（litellm 网络问题） ### Daily Stock Analysis 测试结果 ``` 上 [confidence=0.82 evidence=memory/2026-04-17.md:303-308]
- 清理重复记录时，应该比较 `len(raw_data.income)` 而不是 `report_period`。**report_period 新 ≠ 数据完整**。 ## 数据库状态 - **MongoDB**：`172.25.240.1:27017`，database `tradingagents` - `stock_daily_quotes`：16,554,020 条，最新交易日期 2026-04-29 - `stock_financial_data`：约 4215 条（损坏后），需要重新全量同步 - **注意**：当前数据已损坏，重新同步前不要依赖这些数据做分析 ## 代码修改记录 ### 已修复的文件 1. `tushare.py` 第 1639 行：report_period 强制从 income end_date 获取 2. `financial_data_service.py`：upsert filter 改为 `{symbol, data_source}`（不含 report_period） 3. `main.py`：AsyncIOExecutor 替换 ThreadPoolExecutor ## 今日调度记录 | 时间 | 任务 | 状态 | |------|------|------| | 00:21 | 财务数据同步（Tushare） | ❌ 失败（AsyncIOExecutor bug） | | 08:21 | 历史数据同步（手动） | ✅ 完成 | | 10:23 | 财务数据同步（手动） | ❌ 卡住被僵尸检测终止 | | ~13: [confidence=0.82 evidence=memory/2026-04-30.md:123-156]
