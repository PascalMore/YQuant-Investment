# REM Sleep

### Reflections
- No strong patterns surfaced.

### Possible Lasting Truths
- _apply_existing_delta: fallback 到 stock_pool zone: action = self._zone_delta_action(stock_pool_zone, curr_zone) or "update" [confidence=0.58 evidence=memory/2026-06-01.md:68-68]
- _apply_existing_delta: fallback 到 stock_pool zone: prev_zone = previous.get("pool_zone") or None # "" → None action = self._zone_delta_action(prev_zone, curr_zone) if action is None: stock_pool_zone = existing.get("pool_zone") or None [confidence=0.58 evidence=memory/2026-06-01.md:64-67]
- _zone_delta_action: 空字符串 → None: if not previous_zone or previous_zone not in ZONE_RANK: return None # 而非 "update" [confidence=0.58 evidence=memory/2026-06-01.md:60-61]
