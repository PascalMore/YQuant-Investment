# SPEC-{XX}-{XXX}: {标题}

## 元数据

| 项 | 值 |
|---|---|
| 状态 | Draft/Accepted/Implemented/Deprecated |
| 作者 | YQuant-Codex-Principal |
| 创建日期 | YYYY-MM-DD |
| 最后更新 | YYYY-MM-DD |
| 来源 RFC | RFC-XX-XXX |
| 目标模块 | module |
| 适配 Agent | YQuant-Developer-Engineer, YQuant-Test-Engineer |

## 1. 需求摘要

用 3-5 句话说明本 SPEC 要实现的行为，不重复 RFC 的背景叙述。

## 2. 范围

### 2.1 In Scope

- [ ] 必须实现的行为。

### 2.2 Out of Scope

- [ ] 明确不做的内容。

## 3. 功能规格

| 编号 | 行为 | 输入 | 输出 | 错误/边界 |
|---|---|---|---|---|
| F-001 |  |  |  |  |

## 4. 数据与接口契约

- 数据实体：
- 接口/函数：
- 兼容性约束：
- 幂等性/审计要求：

## 4.bis 持久化契约

如本 SPEC 有任何持久化需求，必须把 RFC 的持久化策略落成可实现、可测试的字段级契约；如无持久化需求，写“无持久化需求”并说明不落盘的数据流。

| 存储对象（库.集合/表/路径） | 字段/索引 | 类型 | 必填 | 默认/派生规则 | 生命周期/TTL | 隐私级别 |
|---|---|---|---|---|---|---|
| 示例：`db.collection` | `id` | string | 是 | 调用方生成，唯一 | 随主记录删除 | L1 |

必须覆盖：
- 每行只描述一个字段、索引或 TTL 规则；索引写明唯一性、组合顺序和排序方向。
- 命名必须与项目数据治理规则一致；跨集合/跨库引用使用稳定引用键（如 `member_ref`），不要写入高敏明文字段。
- 每个字段都要标注隐私级别；L3 字段默认禁止持久化，L2 字段说明最小化、脱敏或匿名化方式。
- 生命周期必须可执行：保留期、归档、软删/硬删、备份恢复、历史兼容策略不能只写“长期保存”。
- 兼容性与迁移：新增字段默认值、旧记录读取策略、schema 版本和迁移失败回滚要求。

## 5. 验收标准

| 编号 | 验收项 | 验证方式 |
|---|---|---|
| A-001 |  |  |

## 6. 测试要求

- 单元测试：
- 集成测试：
- 回归测试：
- 不可自动化验证项：

## 7. 实现约束

- 禁止事项：
- 依赖限制：
- 性能/安全/风控约束：

## 8. 开放问题

- [ ] 待确认问题。

