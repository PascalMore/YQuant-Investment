# DESIGN-{XX}-{XXX}: {标题}

## 元数据

| 项 | 值 |
|---|---|
| 状态 | Draft/Accepted/Implemented/Deprecated |
| 作者 | YQuant-Codex-Principal |
| 创建日期 | YYYY-MM-DD |
| 最后更新 | YYYY-MM-DD |
| 来源 RFC | RFC-XX-XXX |
| 来源 SPEC | SPEC-XX-XXX |
| 目标模块 | module |

## 1. 设计摘要

说明本设计如何满足 SPEC，以及核心取舍。

## 2. 现状分析

- 相关目录：
- 相关文件：
- 现有约束：
- 兼容性风险：

## 3. 方案设计

### 3.1 模块/文件改动

| 文件 | 改动 | 原因 |
|---|---|---|
| path/to/file.py |  |  |

### 3.2 数据流/控制流

```text
input -> service -> repository -> output
```

### 3.3 接口与数据结构

- 新增：
- 修改：
- 废弃：

### 3.4 持久化设计（Persistence Design）

如 SPEC 定义了持久化契约，必须在此把写入路径、过滤策略、错误处理和测试替身设计到文件/函数级；如无持久化需求，写“无持久化需求”。

| 存储对象 | 写入触发点（文件:函数/命令） | 写入字段子集 | 写入前过滤/校验 | 错误处理与回滚 |
|---|---|---|---|---|
| 示例：`db.collection` | `path/to/file.py:function_name()` | `id,status,created_at` | schema 校验；隐私过滤 | 失败返回错误码；不吞异常；可重试/可回滚 |

必须覆盖：
- 写入触发点必须精确到文件、函数、CLI 命令或调度入口；禁止只写“初始化时/流程结束时写入”。
- 字段子集必须与 SPEC §4.bis 对齐；说明哪些字段来自输入、派生、默认值或外部系统。
- 隐私过滤必须落到具体 helper、调用顺序或校验步骤；L3 不入库，L2 最小化或匿名化。
- 错误处理必须映射到 RFC 风险/降级策略：连接失败、权限失败、schema 校验失败、部分写入失败如何处理。
- 测试必须包含 mock/stub client、隐私过滤断言、生命周期/TTL 或迁移兼容验证。

### 3.5 UI/原型设计

如无 UI 改动，写“无”。

## 4. 实现计划

- [ ] Step 1
- [ ] Step 2
- [ ] Step 3

## 5. 测试策略

- 单元测试：
- 集成测试：
- 手工验证：
- 回归范围：

## 6. 风险、降级与回滚

| 风险 | 应对 | 降级/回滚 |
|---|---|---|
|  |  |  |

## 7. 交接给实现者

- 必须遵守：
- 可自行判断：
- 遇到以下情况退回 Principal：

